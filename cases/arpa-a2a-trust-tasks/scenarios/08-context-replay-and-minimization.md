# Scenario 08 — Context replay and privacy minimization

A content-addressed evidence object created in one trust domain is replayed in another, and a full durable actor chain is compared with a privacy-minimized representation sufficient for the same decision.

**Expected:** missing domain/context binding is reported as a replay/misbinding risk. The experiment records whether the relying decision can be satisfied without exposing durable identifiers for every hop.
