# CI trigger

Opening the implementation pull request triggers `Current OpenVTC Track A evidence` because the PR changes the workflow, adapter and case files included in its path filter. The runtime evidence therefore becomes a required observed property of the proposed implementation rather than a post-merge assumption.
