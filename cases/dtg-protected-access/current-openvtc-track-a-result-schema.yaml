expected_result_semantics:
  positive_control:
    equivalent_relationship_binder: identical
    observed_join: detected
    correlator_origin: fixture-supplied
  pressure_case:
    equivalent_relationship_binder: fresh
    relationship_did: absent-on-executed-path
    edge_identifier: absent-on-executed-path
    status_policy: not-evidenced
    trust_task: not-evidenced
    verifier_context: fresh
    observed_join: not-detected
interpretation: >-
  Expected semantics are workflow assertions, not stored runtime results. The actual
  evidence artifact generated by GitHub Actions is authoritative for the execution.
