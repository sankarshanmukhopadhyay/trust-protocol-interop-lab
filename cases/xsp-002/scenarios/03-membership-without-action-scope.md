# Scenario 3 — Federation membership without action scope

The trust chain proves federation membership and metadata resolves, but the requested action is outside the separately granted authority. Membership must not widen scope.
