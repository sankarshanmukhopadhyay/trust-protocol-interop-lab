# Interoperability Observatory

The observatory accepts provenance-preserving signals from portfolio monitoring systems and turns them into **candidate questions**, never automatic interoperability claims.

`dtg/opportunities.yaml` demonstrates the contract. A signal may identify repository convergence, cross-reference, specification movement, or an assurance gap. Human review decides whether to admit a new Interop Case.
